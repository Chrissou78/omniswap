version: '3.8'

services:
  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production

  api:
    build: ./apps/api
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production